package com.example.program.view.Campaign;

import com.example.program.control.MainProgram;
import com.example.program.model.Maps.World1Maps;
import com.example.program.view.AudioPlayer;
import com.example.program.view.Menu.RightPanel;
import javafx.animation.FadeTransition;
import javafx.application.Platform;
import org.junit.Test;
import org.junit.jupiter.api.BeforeAll;
import org.mockito.Mockito;

import java.awt.*;
import java.awt.event.MouseEvent;
import java.io.FileNotFoundException;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.mock;

public class World1TemplateTest {

    @BeforeAll
    static void initJfxRuntime() {
        Platform.startup(() -> {});
    }

    @Test
    public void enteredWallTest() throws FileNotFoundException {
        /*
        MainProgram mainProgram = new MainProgram();
        AudioPlayer audioPlayer = new AudioPlayer();
        World1Maps world1Maps = new World1Maps();
        RightPanel rightPanel = mock(RightPanel.class);

    World1Template world1Template = new World1Template(world1Maps.getLevel15(), 5, 3, mainProgram, rightPanel, 0, audioPlayer, 25);

         */

}

    }

